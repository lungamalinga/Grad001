create table tips(
    id int auto_increment,
    title varchar(255) not null,
    description varchar(255) not null
);


--insert into recycling_tips( title, description )
--values ('Reuse', 'Always reuse stuff');